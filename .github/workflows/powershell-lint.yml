name: 🧹 Auto-Format PowerShell Scripts

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 0' # Every Sunday at 04:00 UTC

jobs:
  build-governance-docs:
    name: Convert & Sign Governance PDFs
    runs-on: windows-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Verify PowerShell 7
        shell: pwsh
        run: |
          Write-Host "PowerShell version:"
          $PSVersionTable.PSVersion

      - name: 📦 Install PSScriptAnalyzer
        run: |
          Install-Module PSScriptAnalyzer -Force -Scope CurrentUser -AllowClobber

      - name: 🧩 Apply Formatting Rules
        shell: pwsh
        run: |
          Import-Module PSScriptAnalyzer
          Write-Host "🧹 Applying analyzer fixes across all PowerShell scripts..."
          Get-ChildItem -Path . -Recurse -Filter '*.ps1' |
            Invoke-ScriptAnalyzer -Fix -Settings './config/PSScriptAnalyzerSettings.psd1' -Verbose |
            Tee-Object -FilePath PSScriptAnalyzerFix.log
          Write-Host "✅ Formatting pass completed."

      - name: 🧾 Commit and Push Changes (if any)
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add '*.ps1'
          if (git diff --cached --quiet) {
            Write-Host "✨ No formatting changes detected."
          } else {
            $date = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'
            git commit -m "🧹 Auto-format PowerShell scripts via CI ($date)"
            git push
            Write-Host "✅ Auto-format commit pushed."
          }

      - name: 📤 Upload Formatter Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: powershell-format-log
          path: PSScriptAnalyzerFix.log
          if-no-files-found: ignore
